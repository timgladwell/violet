# Copy this file to .env.local (gitignored) and fill in real values.
# localdev.sh sources .env.local automatically.
# The same variable names must be set in the Cloudflare Pages dashboard
# (Settings → Environment variables) for staging and production builds.

HUGO_PARAMS_BOOKINGURL=https://your-booking-url-here
