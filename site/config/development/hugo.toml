# Local development (hugo server): build the site as it exists on disk,
# with the full set of content pages and no "coming soon" placeholder.
# See config/production/hugo.toml for the production defaults this overrides.
disableKinds = ['taxonomy', 'term']

[params]
  comingSoon = false
  isStaging = false